# Быстрый старт

## Запуск системы

### 1. Запуск FastAPI сервера

В первом терминале:

```bash
# Установите зависимости (если еще не установлены)
pip install -r requirements.txt

# Запустите API сервер
python api.py
```

Или с помощью uvicorn:

```bash
uvicorn api:app --host 0.0.0.0 --port 8888 --reload
```

API будет доступен по адресу: http://localhost:8888

### 2. Запуск Streamlit приложения

Во втором терминале:

```bash
streamlit run gui_app.py
```

Streamlit приложение будет доступно по адресу: http://localhost:8501

### 3. Использование

1. Откройте браузер и перейдите на http://localhost:8501
2. Убедитесь, что в боковой панели отображается "✅ Подключение к API установлено"
3. Загрузите видеофайл через интерфейс
4. Выделите зоны на первом кадре
5. Нажмите "💾 Сохранить зоны в API"
6. Нажмите "🚀 Запустить анализ"
7. Дождитесь завершения анализа (статус будет обновляться автоматически)
8. Просмотрите результаты

## Проверка работы API

Откройте в браузере:
- Swagger UI: http://localhost:8888/docs
- ReDoc: http://localhost:8888/redoc

## Переменные окружения

Можно настроить URL API через переменную окружения:

```bash
export API_URL=http://localhost:8888
streamlit run gui_app.py
```

Или изменить URL прямо в интерфейсе Streamlit в боковой панели.

## Устранение неполадок

### Ошибка подключения к API

1. Убедитесь, что FastAPI сервер запущен
2. Проверьте URL API в настройках Streamlit
3. Проверьте, что порт 8888 не занят другим приложением

### Ошибка загрузки видео

1. Проверьте формат файла (поддерживаются: mp4, avi, mov, mkv, flv)
2. Убедитесь, что файл не поврежден
3. Проверьте размер файла (слишком большие файлы могут вызвать проблемы)

### Ошибка анализа

1. Проверьте логи FastAPI сервера
2. Убедитесь, что зоны правильно настроены
3. Проверьте, что видео содержит людей (модель YOLO детектирует только людей)

