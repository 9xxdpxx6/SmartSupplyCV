# Анализатор зон магазина

Система анализа видеозаписей с камер наблюдения для определения времени пребывания посетителей в различных зонах магазина.

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

При первом запуске модель YOLOv8n будет автоматически скачана (около 6 МБ).

## Настройка зон

### Способ 1: Интерактивная настройка (рекомендуется)

Запустите интерактивный инструмент для выделения зон мышкой:

```bash
python setup_zones.py
```

**Инструкции:**
1. На экране отобразится первый кадр видео
2. Зажмите левую кнопку мыши и выделите прямоугольник (зона)
3. Отпустите кнопку мыши
4. Введите название зоны в консоли
5. Повторите для всех зон
6. Нажмите **'s'** для сохранения
7. Нажмите **'q'** или **ESC** для выхода

**Дополнительные команды:**
- **'d'** - удалить последнюю зону
- **'c'** - очистить все зоны

Зоны будут сохранены в файл `zones.json` и автоматически загружены при запуске основного скрипта.

### Способ 2: Ручная настройка

Откройте файл `store_zone_analyzer.py` и измените параметры:

```python
# Путь к вашему видеофайлу
VIDEO_PATH = "video.mp4"

# Определение зон магазина (координаты в пикселях)
ZONES_DEFAULT = {
    "У входа": [(100, 50), (300, 200)],
    "Зона А": [(400, 100), (600, 300)],
    "Зона Б": [(200, 350), (450, 500)],
}
```

**Важно:** Координаты зон задаются в пикселях относительно исходного разрешения видео. Скрипт автоматически масштабирует их при уменьшении разрешения для оптимизации.

## Использование

### Способ 1: Графический интерфейс (GUI) - рекомендуется

Запустите Streamlit приложение:
```bash
streamlit run gui_app.py
```

**Возможности GUI:**
- 📁 Загрузка видеофайлов через интерфейс
- 🎯 Интерактивное выделение зон мышкой (drag & drop)
- 📊 Отображение результатов в виде таблицы
- 🖼️ Визуализация с тепловой картой
- 💾 Сохранение зон в JSON

**Инструкция по выделению зон в GUI:**
1. Загрузите видеофайл
2. В разделе "🎯 Выделение зон" зажмите ЛКМ и перетащите для создания прямоугольника
3. Введите название зоны в поле ввода
4. Нажмите "➕ Добавить зону"
5. Повторите для всех зон
6. Скопируйте JSON из компонента (кнопка "📋 Копировать JSON")
7. Вставьте JSON в поле ниже и нажмите "✅ Применить зоны из JSON"
8. Нажмите "🚀 Запустить анализ"

### Способ 2: Консольный скрипт

Запустите скрипт:
```bash
python store_zone_analyzer.py
```

## Результаты

1. **Таблица статистики**:
   - **Зона** - название зоны
   - **Суммарное время (сек)** - общее время, проведенное всеми посетителями в зоне
   - **Среднее время (сек)** - среднее время пребывания одного посетителя
   - **Посетителей** - количество уникальных посетителей (с учетом объединения повторных)

2. **Визуализация** (`zone_analysis_result.png`):
   - Тепловая карта (красный = больше времени)
   - Цветовые зоны (каждая зона свой цвет)
   - Размытые лица для соблюдения 152-ФЗ

## Объединение повторных посетителей

Система автоматически объединяет треки одного и того же человека, если:
- Временной разрыв между треками ≤ 30 секунд
- Размер bounding box схож (рост человека примерно одинаковый)
- Оба трека были в пересекающихся зонах

Это позволяет избежать двойного подсчета одного посетителя, который вышел из кадра и вернулся.

**Настройка параметров объединения** (в `store_zone_analyzer.py`):
```python
MERGE_TRACKS_ENABLED = True  # Включить/выключить объединение
MAX_TRACK_GAP_SECONDS = 30.0  # Максимальный разрыв между треками (секунды)
SIMILAR_SIZE_THRESHOLD = 0.3  # Порог схожести размера (30%)
```

## Параметры оптимизации

Для медленных ПК можно изменить параметры в `store_zone_analyzer.py`:

```python
TARGET_WIDTH = 640      # Целевая ширина (меньше = быстрее)
TARGET_HEIGHT = 480     # Целевая высота
FRAME_SKIP = 5          # Обрабатывать каждый N-й кадр (больше = быстрее)
```

**Рекомендации:**
- Для очень медленных ПК: `TARGET_WIDTH = 480`, `FRAME_SKIP = 10`
- Для средних ПК: `TARGET_WIDTH = 640`, `FRAME_SKIP = 5` (по умолчанию)
- Для быстрых ПК: `TARGET_WIDTH = 1280`, `FRAME_SKIP = 3`

## Соблюдение 152-ФЗ

- Лица автоматически размываются на финальном изображении
- Track ID не сохраняются
- Исходные кадры не сохраняются
- Сохраняется только агрегированная статистика и анонимизированная визуализация

## Требования

- Python 3.8+
- Windows/Linux/macOS
- CPU (GPU не требуется, но рекомендуется для ускорения)

## Примечания

- При пересечении зон время считается параллельно в обеих зонах
- Для определения координат зон можно использовать любой редактор изображений или скриншот видео
- Рекомендуется использовать видео с хорошим освещением и контрастом для лучшего качества трекинга
- Система автоматически объединяет повторные визиты одного посетителя для более точной статистики
- Все зоны сохраняются в `zones.json` и могут быть переиспользованы для разных видео

## Структура проекта

```
supsup-watcher/
├── store_zone_analyzer.py    # Основной скрипт анализа
├── gui_app.py                 # Streamlit GUI приложение
├── setup_zones.py             # Интерактивная настройка зон (OpenCV)
├── components/
│   └── zone_selector_simple.py  # Компонент для выделения зон в GUI
├── zones.json                 # Сохраненные зоны
├── requirements.txt           # Зависимости
└── README.md                  # Этот файл
```

